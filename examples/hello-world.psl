fn count(number: Int) {
    println(number);
    var test = 10;
    test -= 1;
    println(test);
    if(number == 0) {
        println("Done");
    } else {
        count(number -1 );
    }
}

count(800);