import "./multiply";
import "std/io";

import "std/math";

fn sum() {
    const a = 2;
    const b = 3;
    val added = a + b;
    println(added);
}

fn main() {
    const a = 2;
    const b = 3;
    val multiplied = multiply(a, b);
    println(multiplied);
}
