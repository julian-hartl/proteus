fn sum(n: Int) -> Int {
                        var i = n;
                        var result = 0;
                        while true {
                            if i == 0 return result;
                            result += i;
                            i -= 1;
                        }
                    }

fn main() {
                        val sum = sum(90);
}