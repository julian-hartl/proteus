import "std/io";

fn main() {
    var counter = 0;
    var message = "Hello ";
        message += "World";
        counter += 1;
        if counter == 1 {
            message += "!";
        }
    println(message);
}