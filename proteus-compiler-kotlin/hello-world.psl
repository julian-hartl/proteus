val count = 1 * 900 ;

fn recursive(n: Int) {
    println(n);
    if n == 0
        println("Done")
    else
        recursive(n - 1);
}

fn test() {
    if 1 == (2 - 1)
        print("Hi")
    else
        val x = false and true;
}

test();
recursive(count);